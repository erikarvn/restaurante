<%= form_for(@recetum) do |f| %>
  <% if @recetum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recetum.errors.count, "error") %> prohibited this recetum from being saved:</h2>

      <ul>
      <% @recetum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre, 'Nombre' %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :preparacion, 'Preparacion' %><br>
    <%= f.text_area :preparacion %>
  </div>
  <div class="field">
    <%= f.label :ingredientes, 'Ingredientes' %><br>
    <%= f.text_area :ingredientes %>
  </div>
  <div class="actions">
    <%= f.submit 'Registrar receta' %>
  </div>
<% end %>
